/*const config = {
  BASE_URL: "https://cloud.fltt.fr/apps/cloudfloat", // Base chosen URL
  API_ENDPOINT: "/api/v1",
};*/
const config = {
  BASE_URL: window.location.origin+"/apps/dashboard", // Base chosen URL
  API_ENDPOINT: "/api/v1",
};
export default config;
